# Runtime dependencies. Install with `pip install -e .` (pyproject.toml mirrors this list)
# or `pip install -r requirements.txt`. For test/dev tooling see requirements-dev.txt.
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3
python-telegram-bot>=21.0
python-dotenv>=1.0
python-multipart>=0.0.9   # dashboard file uploads (FastAPI form/multipart parsing)
PyYAML>=6.0                # custom slash-command definitions (custom_commands.yaml)
Pillow>=10.0                # document reader: image handling for OCR'd pages
pymupdf>=1.24               # document reader: PDF text extraction + page rasterization (no poppler needed)
python-docx>=1.1            # document reader: .docx
openpyxl>=3.1               # document reader: .xlsx
matplotlib>=3.8             # make_chart: render bar/line/pie/scatter charts to PNG + SVG
markdown>=3.5                # convert_to_pdf: Markdown -> HTML

# Optional extras (imported lazily; the base app runs without them). Install as needed:
#   PDF generation (make_pdf):   pip install -e ".[pdf]"   — needs native GTK/Pango/cairo (hard on Windows)
#   OCR of scanned PDFs/images:  pip install -e ".[ocr]"   — needs the Tesseract binary installed
# weasyprint>=60
# pytesseract>=0.3.10
